# KitchenDashboard Report Issue Button Fix

## Tasks
- [ ] Remove the duplicate orange "Report Issue" button from the issues tab header
- [ ] Remove the unused `showReportIssue` state declaration
- [ ] Remove the entire `{showReportIssue && (...)}` Card section (the inline form)
- [ ] Keep the red "Report Stock Issue" button and the Dialog modal at the bottom
- [ ] Verify the red button opens the Dialog modal correctly
- [ ] Ensure the issues list displays successfully submitted issues
- [ ] Test the form submission via `submitInventoryIssue`
